server:
  port: ${SERVER_PORT:3000}

spring:

  application:
    name: bff

  kafka:
    bootstrap-servers: ${KAFKA_BROKER:localhost:9092}
    consumer:
      group-id: bff-group
      auto-offset-reset: latest

logging:
  level:
    org:
      apache:
        kafka: OFF
